services:
  - type: web
    name: yoga-pose-library
    env: docker
    dockerfilePath: ./Dockerfile
    plan: free
    envVars:
      - key: SPRING_PROFILES_ACTIVE
        value: prod
      - key: DATABASE_URL
        fromDatabase:
          name: yoga-pose-db
          property: connectionString
    healthCheckPath: /actuator/health

databases:
  - name: yoga-pose-db
    databaseName: yoga_poses
    plan: free