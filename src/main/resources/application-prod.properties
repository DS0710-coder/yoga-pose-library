# Server Configuration
server.port=${PORT:8080}

# Critical: Database URL parsing
# Render provides URL in this format: postgresql://user:pass@host:5432/db
spring.datasource.url=${postgresql://yoga_postgre_4y1i_user:vXcI7DhtLd13rcMm4NkqQbP7t42fXAB1@dpg-d41uvdi4d50c73ffima0-a/yoga_postgre_4y1i}
spring.datasource.driver-class-name=org.postgresql.Driver

# IMPORTANT: Don't set username/password separately
# They are already in the DATABASE_URL

# JPA Configuration
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Critical: These properties fix common Render/Postgres issues
spring.jpa.properties.hibernate.jdbc.lob.non_contextual_creation=true
spring.jpa.properties.hibernate.temp.use_jdbc_metadata_defaults=false
spring.jpa.open-in-view=false

# Connection Pool
spring.datasource.hikari.connection-timeout=30000
spring.datasource.hikari.maximum-pool-size=2
spring.datasource.hikari.minimum-idle=1
spring.datasource.hikari.idle-timeout=600000
spring.datasource.hikari.max-lifetime=1800000

# Detailed logging to debug the issue
logging.level.root=INFO
logging.level.com.example.guide=DEBUG
logging.level.org.springframework.jdbc=DEBUG
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE
logging.level.com.zaxxer.hikari.HikariConfig=DEBUG
logging.level.com.zaxxer.hikari=TRACE